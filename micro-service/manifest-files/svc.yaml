apiVersion: v1
kind: Service
metadata:
  name: myapp
spec:
  selector:
    app: static-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30070
  type: NodePort