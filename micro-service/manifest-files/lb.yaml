apiVersion: v1
kind: Service
metadata:
  name: myapp
spec:
  selector:
    app: static-app
  ports:
    - protocol: TCP
      port: 80       # External port
      targetPort: 5000  # Container port
  type: LoadBalancer
